# Loops

Loops repeats something multiple times automatically until it hit the right condition

### Different kinds of Loops

    - For loop
    - While Loop
    - Do-While Loop

---

We can also loop an array to iterate and display each index of array.
We usually use foreach for iterating an array.

---

We can also loop through an object but that's for advance level
